#include <stdio.h>
int main ()
{
    int n = 10;
    if (n < 30) {
        printf("n is less than thirty\n");
    }
    else if (n < 50) {
        printf("n is less than fifty\n");
    }
    return 0;
}
